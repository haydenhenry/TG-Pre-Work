let numberClusters = [[1, 2], [3, 4], [5, 6]]

const target = numberClusters[2][1];

// In this lesson I learned how to create nested arrays, or arrays within arrays. The items within the nested arrays can be called by
// chaining the index numbers of both the nested array, and the array item together after the array name in square brackets. 
