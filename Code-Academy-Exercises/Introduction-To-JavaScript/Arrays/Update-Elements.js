let groceryList = ['bread', 'tomatoes', 'milk'];

groceryList[1] = 'avocados';

// Using the index of an array item, we can change its value by using the name of the array, followed by the index number. We then
// equate the value to a different one.
