class Surgeon {
  constructor(name, department) {
    this.name = name;
    this.department = department;
  }
}

// The method that sets classes and objects is the constructor() method, which is called every time JavaScript creates a new instance of a class.
