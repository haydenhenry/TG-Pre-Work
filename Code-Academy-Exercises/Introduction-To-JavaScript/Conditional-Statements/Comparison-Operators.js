let hungerLevel = 7;
if(hungerLevel > 7 ) {
  console.log('Time to eat!');
} else {
  console.log('We can eat later!');
}

// A comparison operator is an operator that compares two values and returns a value of true or false. 
