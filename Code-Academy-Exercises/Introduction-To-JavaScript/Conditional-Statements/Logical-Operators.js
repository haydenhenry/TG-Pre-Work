let mood = 'sleepy';
let tirednessLevel = 6;

if( mood === "sleepy" && tirednessLevel > 8) {
  console.log("time to sleep");
} else {
  console.log('not bed time yet');
}

/* Logical operators add more sophisticated logic to a conditional statement. These can be used to check if multiple conditions return true
or false. These include the and operator &&, the or operator ||, and the not operator !. */
