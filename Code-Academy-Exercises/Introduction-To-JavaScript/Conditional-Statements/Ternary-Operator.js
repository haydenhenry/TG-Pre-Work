let isLocked = false;

isLocked ? console.log('You will need a key to open the door.') : console.log('You will not need a key to open the door.');

let isCorrect = true;

isCorrect ? console.log('Correct!') : console.log('Incorrect!');

let favoritePhrase = 'Love That!';

favoritePhrase === 'Love That!' ? console.log('I love that!') : console.log("I don't love that!");


/* A ternary operator can be used as shorthand for an if-else statement. An operator like ? is placed after the condition. Then two values are
given, separated by a colon. This will check the first statement for a true/truthy value, and if it returns false, it will give the
second value. */
