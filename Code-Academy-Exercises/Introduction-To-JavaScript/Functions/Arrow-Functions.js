const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

// Arrow functions are a shorthand way to declare a function without using the keyword function. They include the parameters followed
// by a fat arrow pointing to the code wrapped in curly braces () => .
