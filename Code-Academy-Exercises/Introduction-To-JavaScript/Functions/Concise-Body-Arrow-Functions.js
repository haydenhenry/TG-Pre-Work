const plantNeedsWater = day => day === 'Wednesday' ? true : false;

// Arrow functions can be written concisely with the knowledge of the syntax. Functions that take a single parameter do not need
// the parameter enclosed in parentheses, but functions with zero or multiple parameters require them. Also, a function body that is a
// single line block does not need to be enclosed in curly braces. The results of the expression will be automatically returned, this is called
// implicit return.
