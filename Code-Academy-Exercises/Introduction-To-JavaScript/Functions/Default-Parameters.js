function makeShoppingList(item1 = 'milk', item2 = 'bread', item3 = 'eggs'){
  console.log(`Remember to buy ${item1}`);
  console.log(`Remember to buy ${item2}`);
  console.log(`Remember to buy ${item3}`);
}


// Javascript ES6 allows for the use of default parameters, which are parameters you can set as the default in the event that no parameter is passed
// to the function. When a function is given a parameter, the given parameter overrides the default parameters.
