function getReminder() {
  console.log('Water the plants');
}

function greetInSpanish() {
  console.log('Buenas Tardes');
}

/* One way to create a function is a function declaration, which binds the function to a name. It consists of the function keyword, 
an identifier followed by parentheses, and a function body wrapped in curly braces. It is also bad practice to call a function before
 it is defined. */
