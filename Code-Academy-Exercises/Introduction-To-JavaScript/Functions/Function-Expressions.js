const plantNeedsWater = function(day) {
  if (day === "Wednesday") {
      return true;
      } else {
        return false;
      }
}

console.log(plantNeedsWater("Tuesday"));


// Function expressions are functions that are declared within an expression. These functions are usually written without an identifier, making
// it an anonymous function. These are usually stored inside a variable so it can be called.
