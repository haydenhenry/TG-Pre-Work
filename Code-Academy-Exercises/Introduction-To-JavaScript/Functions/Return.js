function monitorCount( rows , columns ) {
  return rows * columns;
}
const numOfMonitors = monitorCount(5, 4);

console.log(numOfMonitors);

// The return keyword passes information from a function call back to the user. Once the return keyword is used, the function stops running
// the code. 
