const vacationSpots = ['Bali', 'Paris', 'Tulum'];

// Write your code below
for (let i = 0; i < vacationSpots.length; i++) {
   console.log('I would love to visit ' + vacationSpots[i]);
};


// We can set a loop to log through each element of an array using the length property. We can set our iterater variable to represent
// each element in the array, and go through the entire list item by item.
