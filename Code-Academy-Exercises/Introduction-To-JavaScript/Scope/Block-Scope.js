function logVisibleLightWaves() {
  const lightWaves = 'Moonlight';
  console.log(lightWaves);
}

logVisibleLightWaves();

console.log(lightWaves);

// Block scope refers to variables that are only accessible from within the same block of code they reside in. If they are called outside
// of the block scope, a reference error will appear. 
