const satellite = 'The Moon';
const galaxy = 'The Milky Way';
const stars = 'North Star';

function callMyNightSky() {
  return 'Night Sky: ' + satellite + ', ' + stars + ', and ' + galaxy;
}
console.log(callMyNightSky());

// Scope refers to whether variables exist within or outside of a block. Global scope refers to things outside of a block, that are available
// globally, or are accessible anywhere in the program. Variables outside of a code block are global variables.
